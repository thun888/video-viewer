# 视频展示平台

## 命令行

> 先安装依赖 npm install

开发: npm run dev

构建: npm run build

运行: npm run start

## 维护

available via `npm run`:
  dev
    next dev
  build
    next build
  lint
    eslint
  db:generate
    drizzle-kit generate
  db:migrate
    drizzle-kit migrate
  db:seed
    tsx db/seed.ts